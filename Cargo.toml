[package]
name = "m2"
version = "0.1.0"
edition = "2021"

# Base foundation - minimal dependencies
[[bin]]
name = "m2_base"
path = "m2_base.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
